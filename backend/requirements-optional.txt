# Заавал биш сангууд — интернэтгүй серверт суулгах шаардлагагүй.
#
# cryptography is also required by requirements.txt for encrypted hospital API
# credentials. Keep this compatibility pin for existing installation commands.
#
# Суулгах (интернэттэй сервер дээр):
#   venv/bin/pip install -r requirements-optional.txt
cryptography==50.0.0   # PYSEC-2026-3552 засагдсан (49.0.0 эмзэг байсан)
